### 2.4.代码

标签：
```
 <code> 标签包裹内联样式的代码片段。
 <kbd> 标签标记用户通过键盘输入的内容。
 <pre> 多行代码可以使用标签。为了正确的展示代码，注意将尖括号做转义处理。
.pre-scrollable 类，其作用是设置 max-height 为 350px ，并在垂直方向展示滚动条。
<var>标签标记变量。
<samp> 标签来标记程序输出的内容。

```